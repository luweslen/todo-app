import { View } from "react-native";
import { listInfoStyles } from "./styles";

export type ListInfoProps = {};

export function ListInfo({ }: ListInfoProps) {
  return (
    <View style={listInfoStyles.container}>
    </View>
  );
}